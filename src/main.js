import './style.css';
import { products, categories } from './products.js';

const app = document.querySelector('#app');
const money = n => new Intl.NumberFormat('uk-UA').format(n) + ' ₴';
const icon = (name) => `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.35" aria-hidden="true">${({search:'<circle cx="10.5" cy="10.5" r="6.5"/><path d="m16 16 5 5"/>',heart:'<path d="M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.7l-1.1-1.1a5.5 5.5 0 0 0-7.8 7.8L12 21l8.8-8.6a5.5 5.5 0 0 0 0-7.8Z"/>',bag:'<path d="M5 7h14l1 14H4L5 7Z"/><path d="M8 8V6a4 4 0 0 1 8 0v2"/>',arrow:'<path d="M3 12h17m-6-6 6 6-6 6"/>',close:'<path d="m5 5 14 14M5 19 19 5"/>',menu:'<path d="M3 7h18M3 16h18"/>',plus:'<path d="M12 4v16M4 12h16"/>'})[name] || ''}</svg>`;
const read = key => {try {const value=JSON.parse(localStorage.getItem(key)||'[]');return Array.isArray(value)?value.filter(id=>products.some(p=>p.id===id)):[]} catch {return []}};
let favorites = read('quiet-favorites'), cart = read('quiet-cart');
let filter = {query:'',category:'Все',max:16000,sort:'curated',unique:false,new:false};
let currentProduct, gallery=[],galleryIndex=0,returnFocus;
const escapeHTML = value => String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const logo = '<span class="brand-title">тихие истории<span class="brand-star">✳</span></span><span class="brand-subtitle">ГАЛЕРЕЯ МАЛЕНЬКИХ МИРОВ</span>';
function header(home=false){return `<header class="header ${home?'over-hero':''}"><a class="brand" href="#/" aria-label="Тихие истории — главная">${logo}</a><nav class="desktop-nav"><a href="#/collection">Коллекция</a><a href="#/collection?new">Новинки <i></i></a><a href="#/stories">Истории</a><a href="#/about">О бренде</a></nav><div class="header-actions"><button data-action="search" aria-label="Поиск">${icon('search')}</button><a href="#/favorites" aria-label="Избранное">${icon('heart')}<span class="count" data-count="favorites">${favorites.length||''}</span></a><a href="#/cart" aria-label="Корзина">${icon('bag')}<span class="count" data-count="cart">${cart.length||''}</span></a><button class="menu-button" data-action="menu" aria-label="Открыть меню" aria-expanded="false">${icon('menu')}</button></div><nav class="mobile-nav" hidden><button data-action="search">Поиск по коллекции</button><a href="#/collection">Коллекция</a><a href="#/collection?new">Новинки</a><a href="#/stories">Истории</a><a href="#/about">О бренде</a></nav></header>`}
function footer(){return `<footer><div class="footer-top"><a class="brand" href="#/">${logo}</a><p>Коллекция, созданная для тех,<br>кто замечает детали.</p><div><a href="#/collection">Коллекция</a><a href="#/about">О бренде</a></div><div><a href="#/delivery">Доставка</a><a href="#/contacts">Контакты и социальные сети</a></div></div><div class="footer-bottom"><span>© 2026 Тихие истории</span><span>Демонстрационная коллекция</span><a href="#/privacy">Политика конфиденциальности</a><a href="#/" class="back-top">Наверх ↑</a></div></footer>`}
function card(p,cls=''){return `<article class="product-card ${cls}"><div class="card-picture"><a href="#/product/${p.id}" aria-label="Подробнее: ${p.name}"><img src="${p.images[0]}" alt="${p.name} — фарфоровая статуэтка" loading="lazy"/><span class="card-overlay"><span>${p.category}</span><strong>${p.name}</strong><span>${money(p.price)}</span><span class="detail-link">Подробнее ${icon('arrow')}</span></span></a><span class="edition">${p.unique?'ЕДИНСТВЕННЫЙ ЭКЗЕМПЛЯР':p.isNew?'НОВОЕ В КОЛЛЕКЦИИ':'В НАЛИЧИИ'}</span><button class="favorite ${favorites.includes(p.id)?'selected':''}" data-favorite="${p.id}" aria-label="${favorites.includes(p.id)?'Убрать из избранного':'В избранное'}: ${p.name}" aria-pressed="${favorites.includes(p.id)}">${icon('heart')}</button></div><a class="card-caption" href="#/product/${p.id}"><div><h3>${p.name}</h3><p>${p.category}</p></div><span>${money(p.price)}</span></a></article>`}
function home(){return `<section class="hero"><img class="hero-image" src="/images/hero.webp" alt="Фарфоровая девушка с оленёнком в мягком свете" fetchpriority="high"/><div class="hero-shade"></div><div class="hero-content"><p class="eyebrow">ИСКУССТВО БЫТЬ БЛИЖЕ</p><h1>Истории, которые<br>можно <em>держать</em><br>в руках.</h1><p class="hero-description">Авторские декоративные статуэтки<br>и коллекционные композиции</p><a class="button light" href="#/collection">Смотреть коллекцию ${icon('arrow')}</a></div><div class="hero-bottom"><span>Коллекционные произведения <b>·</b> Ручная работа <b>·</b> Уникальные композиции</span><a href="#/product/girl-and-fawn" class="hero-caption"><span>01 / 06</span><span>Девушка с оленёнком<small>Фарфор · Ручная роспись</small></span>${icon('arrow')}</a></div><a class="scroll-cue" href="#/?section=collection">ЛИСТАЙТЕ, ЧТОБЫ ОТКРЫТЬ <span>↓</span></a></section>
<section class="intro reveal"><p class="eyebrow">МАЛЕНЬКИЕ ВЕЩИ. БОЛЬШИЕ ЧУВСТВА.</p><h2>Искусство, которому<br>найдётся место <em>дома.</em></h2><p>В тонких линиях, нежной росписи и едва заметном жесте —<br>целый мир. Собираем вещи, рядом с которыми хочется замедлиться.</p><span class="ornament">✳</span></section>
<section class="section collection-section" id="collection"><div class="section-heading reveal"><div><p class="eyebrow">ВЫБРАНО С ЧУВСТВОМ</p><h2>Знакомство с <em>коллекцией</em></h2></div><a class="text-link" href="#/collection">Вся коллекция <sup>06</sup> ${icon('arrow')}</a></div><div class="editorial-grid">${[products[0],products[1],products[2]].map((p,i)=>card(p,i===0?'large':'')).join('')}</div></section>
<section class="story-section section reveal" id="stories"><div class="story-image"><img src="/images/product-5.webp" alt="Фарфоровая композиция матери и дочери" loading="lazy"/><span>БЛИЗОСТЬ, ЗАСТЫВШАЯ В ФАРФОРЕ</span></div><div class="story-copy"><p class="eyebrow">БОЛЬШЕ, ЧЕМ ПРЕДМЕТ</p><h2>У каждой фигурки<br>есть своя <em>история.</em></h2><p>Это не просто декоративные предметы. Каждая композиция создаёт маленький мир, в котором есть персонажи, настроение и собственная история.</p><p>Одни напоминают о детстве. Другие — о дорогом человеке. А некоторые просто становятся частью вашего дома.</p><a href="#/stories" class="text-link">Узнать больше ${icon('arrow')}</a></div></section>
<section class="section categories-section"><div class="section-heading"><div><p class="eyebrow">НАЙДИТЕ СВОЙ СЮЖЕТ</p><h2>Миры <em>в миниатюре</em></h2></div><span class="muted">Листайте и выбирайте →</span></div><div class="category-strip">${categories.map((c,i)=>`<a class="category" href="#/collection?category=${encodeURIComponent(c)}"><img src="${products[[0,1,2,3,4,5,0][i]].images[0]}" alt="${c}" loading="lazy"/><span><small>0${i+1}</small>${c}${icon('arrow')}</span></a>`).join('')}</div></section>
<section class="unique-section reveal"><div class="vertical-label">НЕ МАССОВОЕ ПРОИЗВОДСТВО</div><img src="/images/product-6.webp" alt="Коллекционная фарфоровая балерина" loading="lazy"/><div><p class="eyebrow">ОДНА ВЕЩЬ — ОДНА ИСТОРИЯ</p><h2>Красота —<br>это <em>чувство.</em></h2><p>Некоторые вещи становятся ценными не потому, что они редкие, а потому что они вызывают чувство.</p><a class="text-link" href="#/collection?unique">Посмотреть уникальные экземпляры ${icon('arrow')}</a></div></section>
<section class="section"><div class="section-heading"><div><p class="eyebrow">МАЛЕНЬКАЯ ЛИЧНАЯ ВЫСТАВКА</p><h2>Особенные <em>экземпляры</em></h2></div><div class="carousel-controls"><button data-slide="-1" aria-label="Предыдущие экземпляры">←</button><button data-slide="1" aria-label="Следующие экземпляры">→</button></div></div><div class="featured-strip">${products.filter(p=>p.featured).map(p=>card(p)).join('')}</div></section>`}
function collection(){return `<section class="page section"><p class="eyebrow">ГАЛЕРЕЯ МАЛЕНЬКИХ МИРОВ</p><h1>${filter.new?'Новое в коллекции':filter.unique?'Единственные в своём роде':'Ваша следующая'} ${filter.new||filter.unique?'':'<em>история.</em>'}</h1><p class="page-lead">Выбирайте сердцем. Рассматривайте не спеша.</p><div class="filters"><label class="search-field">${icon('search')}<input id="catalog-search" type="search" placeholder="Название статуэтки" aria-label="Поиск по названию" value="${escapeHTML(filter.query)}"/></label><div class="price-filter"><label for="price">Цена до <strong id="price-value">${money(filter.max)}</strong></label><input id="price" type="range" min="0" max="16000" step="50" value="${filter.max}"/></div><label class="sort-label">Порядок<select id="sort"><option value="curated">Выбор галереи</option><option value="asc">Сначала дешевле</option><option value="desc">Сначала дороже</option><option value="name">По названию</option><option value="new">Сначала новинки</option></select></label></div><div class="category-filters">${['Все',...categories].map(c=>`<button data-category="${c}" class="${filter.category===c?'active':''}">${c}</button>`).join('')}</div><div class="result-meta"><span id="result-count"></span><button data-action="reset" class="reset">Сбросить фильтры ↺</button></div><div id="results" class="catalog-grid"></div></section>`}
function filtered(){let list=products.filter(p=>(p.name.toLocaleLowerCase('ru').replaceAll('ё','е').includes(filter.query.toLocaleLowerCase('ru').replaceAll('ё','е')))&&(filter.category==='Все'||p.category===filter.category||p.tags.includes(filter.category))&&p.price<=filter.max&&(!filter.unique||p.unique)&&(!filter.new||p.isNew));if(filter.sort==='asc')list.sort((a,b)=>a.price-b.price);if(filter.sort==='desc')list.sort((a,b)=>b.price-a.price);if(filter.sort==='name')list.sort((a,b)=>a.name.localeCompare(b.name,'ru'));if(filter.sort==='new')list.sort((a,b)=>Number(b.isNew)-Number(a.isNew));return list}
function results(){const list=filtered();document.querySelector('#results').innerHTML=list.length?list.map(p=>card(p)).join(''):empty('Ничего не нашлось','Попробуйте изменить название, категорию или цену.','Сбросить фильтры','reset');document.querySelector('#result-count').textContent=`Найдено: ${list.length} из ${products.length}`;document.querySelector('#price-value').textContent=money(filter.max);document.querySelector('#sort').value=filter.sort}
function product(p){currentProduct=p;return `<section class="product-page section"><a href="#/collection" class="breadcrumb">← Назад в коллекцию</a><div class="product-presentation"><div class="product-visual"><button class="main-photo" data-gallery="main" aria-label="Открыть фотографию на весь экран"><img src="${p.images[0]}" alt="${p.name}"/><span>${icon('plus')} Рассмотреть ближе</span></button></div><div class="product-info"><p class="eyebrow">КОЛЛЕКЦИОННАЯ КОМПОЗИЦИЯ · ${String(products.indexOf(p)+1).padStart(2,'0')}</p><h1>${p.name}</h1><p class="availability">● ${p.available?'В наличии':'Нет в наличии'}${p.unique?' · Единственный экземпляр':''}</p><p class="product-price">${money(p.price)}</p><div class="purchase-actions"><button class="button dark" data-cart="${p.id}" ${!p.available?'disabled':''}>${cart.includes(p.id)?'В вашей коллекции':'Добавить в коллекцию'} ${icon('bag')}</button><button class="favorite inline ${favorites.includes(p.id)?'selected':''}" data-favorite="${p.id}" aria-pressed="${favorites.includes(p.id)}" aria-label="В избранное: ${p.name}">${icon('heart')}</button></div><dl>${[['Материал',p.material],['Высота',p.height+' см'],['Ширина',p.width+' см'],['Год',p.year],['Состояние',p.condition],['Происхождение',p.origin]].map(([k,v])=>`<div><dt>${k}</dt><dd>${v}</dd></div>`).join('')}</dl><p class="product-note">Демонстрационный экземпляр. Изображение и характеристики иллюстрируют концепцию галереи.</p><a href="#/delivery" class="text-link">О доставке и заботе о фарфоре ${icon('arrow')}</a></div></div><div class="product-story"><p class="eyebrow">ИСТОРИЯ ЭТОЙ КОМПОЗИЦИИ</p><h2>Маленькая сцена, в которой<br><em>остановилось мгновение.</em></h2><p>${p.description}</p></div><div class="section-heading"><div><p class="eyebrow">КРАСОТА В КАЖДОМ ШТРИХЕ</p><h2>Рассмотрите <em>детали</em></h2></div><span class="muted">Нажмите, чтобы приблизить</span></div><div class="macro-grid">${p.additionalImages.map((im,i)=>`<button class="macro" data-gallery="${i}" aria-label="Увеличить: ${im.label}"><img src="${im.src}" alt="${im.label}" style="object-position:${im.position};transform:scale(${im.zoom});transform-origin:${im.position}"/><span>${im.label} ${icon('plus')}</span></button>`).join('')}</div><div class="section-heading related-heading"><h2>Рядом в <em>коллекции</em></h2></div><div class="related-grid">${products.filter(x=>x.id!==p.id).slice(0,3).map(x=>card(x)).join('')}</div></section>`}
function empty(title,copy,label='Открыть коллекцию',action=''){return `<div class="empty"><span>✳</span><h2>${title}</h2><p>${copy}</p>${action?`<button class="button dark" data-action="${action}">${label}</button>`:`<a class="button dark" href="#/collection">${label} ${icon('arrow')}</a>`}</div>`}
function favoritesPage(){return `<section class="page section"><p class="eyebrow">ВАШ ЛИЧНЫЙ ВЫБОР</p><h1>Близкое <em>сердцу.</em></h1><p class="page-lead">Истории, к которым хочется вернуться.</p>${favorites.length?`<div class="catalog-grid">${products.filter(p=>favorites.includes(p.id)).map(p=>card(p)).join('')}</div>`:empty('Пока здесь тихо','Сохраните понравившуюся фигурку, нажав на сердечко.')}</section>`}
function cartPage(){const list=cart.map(id=>products.find(p=>p.id===id));return `<section class="page section"><p class="eyebrow">НОВАЯ ГЛАВА ВАШЕГО ДОМА</p><h1>Ваша <em>коллекция.</em></h1>${list.length?`<div class="cart-layout"><div>${list.map(p=>`<article class="cart-item"><a href="#/product/${p.id}"><img src="${p.images[0]}" alt="${p.name}"/></a><div><p class="eyebrow">${p.category}</p><a href="#/product/${p.id}"><h2>${p.name}</h2></a><p>${p.unique?'Единственный экземпляр':'1 экземпляр'}</p><button class="text-link" data-remove="${p.id}">Убрать из коллекции</button></div><strong>${money(p.price)}</strong></article>`).join('')}</div><aside class="cart-summary"><p class="eyebrow">ВАШ ВЫБОР</p><p>Экземпляров <span>${list.length}</span></p><h3>Итого <span>${money(list.reduce((a,p)=>a+p.price,0))}</span></h3><p class="muted">Стоимость доставки рассчитывается отдельно.</p><button class="button dark" data-action="export">Сохранить подборку ${icon('arrow')}</button><p class="product-note">Это демонстрационная галерея. Оплата и отправка заказов пока не подключены. Подборку можно сохранить файлом.</p></aside></div>`:empty('История только начинается','Найдите фигурку, которой захочется подарить место в вашем доме.')}</section>`}
function about(stories=false){return `<section class="page section about-page"><p class="eyebrow">${stories?'ИСТОРИИ КОЛЛЕКЦИИ':'О ГАЛЕРЕЕ'}</p><h1>${stories?'У каждой фигурки<br>есть своя <em>история.</em>':'Маленький музей,<br>который можно<br>открыть <em>дома.</em>'}</h1><div class="about-editorial"><img src="/images/hero.webp" alt="Фарфоровая композиция в свете окна"/><div><p class="eyebrow">ТИХИЕ ИСТОРИИ</p><h2>Замечать <em>прекрасное.</em></h2><p>Нас привлекают вещи, которые не требуют внимания, но удерживают взгляд. Тонкий изгиб руки. Цветок размером с каплю. Оттенок глазури, который меняется вместе со светом.</p><p>«Тихие истории» — концепция галереи небольших декоративных статуэток. Здесь у каждой композиции есть пространство, чтобы раскрыть свой характер, а у вас — время, чтобы рассмотреть её.</p><p>Коллекционирование начинается с чувства. С маленького узнавания: эта история — моя.</p><a href="#/collection" class="text-link">Найти свою историю ${icon('arrow')}</a></div></div>${stories?products.map((p,i)=>`<article class="story-entry"><span class="eyebrow">0${i+1}</span><a href="#/product/${p.id}"><h2>${p.name}</h2></a><p>${p.description}</p><a href="#/product/${p.id}" class="text-link">Рассмотреть композицию ${icon('arrow')}</a></article>`).join(''):''}</section>`}
const information={delivery:['Доставка и забота','Каждой вещи — бережное отношение.','В этой демонстрационной версии доставка и оформление заказов ещё не подключены. Способы, сроки, стоимость доставки и условия возврата будут добавлены вместе с реальным каталогом.','Уход за фарфором','Удаляйте пыль мягкой сухой кистью. Избегайте абразивных средств, резких перепадов температуры и давления на тонкие лепные детали. Особенности ухода за конкретным изделием уточняйте у его автора.'],contacts:['Остаёмся на связи','Хорошие истории начинаются со знакомства.','Контакты владельца галереи и официальные страницы в социальных сетях будут размещены перед открытием продаж.','Пока можно собрать свою коллекцию','Сохраняйте понравившиеся композиции в избранное или добавляйте в корзину. Подборка останется в этом браузере.'],privacy:['Конфиденциальность','Ваш личный выбор остаётся вашим.','Этот frontend хранит только идентификаторы избранных товаров и корзины в localStorage вашего браузера. Форм сбора персональных данных, аналитики и платёжного сервиса нет.','Управление данными','Удалить сохранённые данные можно кнопкой ниже или через настройки данных сайта в браузере. Перед запуском продаж владельцу потребуется подготовить политику с учётом фактически подключённых сервисов.']};
function info(type){const [title,lead,body,sub,more]=information[type];return `<section class="page section info-page"><p class="eyebrow">ТИХИЕ ИСТОРИИ</p><h1>${title}</h1><h2>${lead}</h2><p>${body}</p><h2>${sub}</h2><p>${more}</p>${type==='privacy'?'<button class="text-link" data-action="clear-data">Удалить избранное и корзину</button>':'<a class="text-link" href="#/collection">Вернуться к коллекции →</a>'}</section>`}
function route(){document.querySelector('dialog')?.close();document.body.classList.remove('modal-open');const raw=location.hash.slice(1)||'/';const [path,query]=raw.split('?');let content;currentProduct=null;if(path==='/'){content=home()}else if(path==='/collection'){const params=new URLSearchParams(query);filter={query:params.get('q')||'',category:params.get('category')||'Все',max:16000,sort:'curated',unique:params.has('unique'),new:params.has('new')};content=collection()}else if(path.startsWith('/product/')){const p=products.find(p=>p.id===path.split('/')[2]);content=p?product(p):`<section class="page section">${empty('История не найдена','Вернитесь в коллекцию, чтобы выбрать другой экземпляр.')}</section>`}else if(path==='/favorites')content=favoritesPage();else if(path==='/cart')content=cartPage();else if(path==='/about'||path==='/stories')content=about(path==='/stories');else if(information[path.slice(1)])content=info(path.slice(1));else content=`<section class="page section">${empty('Здесь пока нет истории','Эта страница не найдена.')}</section>`;app.innerHTML=header(path==='/')+`<main>${content}</main>`+footer()+`<div class="toast" role="status" aria-live="polite"></div><dialog id="lightbox" aria-label="Просмотр фотографий"><button class="lightbox-close" data-action="close" aria-label="Закрыть просмотр">${icon('close')}</button><button class="lightbox-prev" data-lightbox-step="-1" aria-label="Предыдущая фотография">←</button><div class="lightbox-image"></div><button class="lightbox-next" data-lightbox-step="1" aria-label="Следующая фотография">→</button><p class="lightbox-caption"></p></dialog><dialog id="search-dialog" aria-label="Поиск по коллекции"><button data-action="close" class="search-close" aria-label="Закрыть поиск">${icon('close')}</button><p class="eyebrow">НАЙДИТЕ СВОЮ ИСТОРИЮ</p><h2>Что вам <em>близко?</em></h2><form id="search-form"><label class="search-field">${icon('search')}<input type="search" name="q" placeholder="Например, девушка или сад" aria-label="Название статуэтки" required/></label><button class="button dark">Найти ${icon('arrow')}</button></form></dialog>`;if(path==='/collection')results();document.title=currentProduct?`${currentProduct.name} — Тихие истории`:'Тихие истории — галерея коллекционных статуэток';window.scrollTo({top:0,behavior:'instant'});if(path==='/'&&new URLSearchParams(query).get('section')==='collection')requestAnimationFrame(()=>document.querySelector('#collection')?.scrollIntoView({behavior:'smooth'}));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));document.querySelectorAll('dialog').forEach(d=>{d.addEventListener('close',()=>{document.body.classList.remove('modal-open');returnFocus?.focus()});d.addEventListener('click',e=>{if(e.target===d)d.close()})})}
function persist(){try{localStorage.setItem('quiet-favorites',JSON.stringify(favorites));localStorage.setItem('quiet-cart',JSON.stringify(cart))}catch{toast('Браузер не разрешил сохранить подборку. Она доступна до закрытия страницы.')}document.querySelector('[data-count="favorites"]').textContent=favorites.length||'';document.querySelector('[data-count="cart"]').textContent=cart.length||''}
let toastTimer;function toast(message){const t=document.querySelector('.toast');t.textContent=message;t.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>t.classList.remove('show'),3200)}
function openDialog(id){returnFocus=document.activeElement;document.querySelector(id).showModal();document.body.classList.add('modal-open')}
function drawGallery(){const im=gallery[galleryIndex];document.querySelector('.lightbox-image').innerHTML=`<img src="${im.src}" alt="${im.label}" style="object-position:${im.position||'center'};transform:scale(${im.zoom||1});transform-origin:${im.position||'center'}"/>`;document.querySelector('.lightbox-caption').textContent=`${galleryIndex+1} / ${gallery.length} · ${im.label}`}
document.addEventListener('click',e=>{const target=e.target.closest('button');if(!target)return;if(target.dataset.favorite){const id=target.dataset.favorite;favorites=favorites.includes(id)?favorites.filter(x=>x!==id):[...favorites,id];persist();if(location.hash==='#/favorites'){route();return}document.querySelectorAll(`[data-favorite="${id}"]`).forEach(b=>{b.classList.toggle('selected',favorites.includes(id));b.setAttribute('aria-pressed',favorites.includes(id));b.setAttribute('aria-label',`${favorites.includes(id)?'Убрать из избранного':'В избранное'}: ${products.find(p=>p.id===id).name}`)});toast(favorites.includes(id)?'История сохранена в избранном':'История убрана из избранного')}
if(target.dataset.cart){if(!cart.includes(target.dataset.cart))cart.push(target.dataset.cart);persist();target.innerHTML=`В вашей коллекции ${icon('bag')}`;toast('Добавлено в корзину — ваша коллекция пополняется')}
if(target.dataset.remove){cart=cart.filter(x=>x!==target.dataset.remove);persist();route()}
if(target.dataset.category){filter.category=target.dataset.category;document.querySelectorAll('[data-category]').forEach(b=>b.classList.toggle('active',b.dataset.category===filter.category));results()}
if(target.dataset.slide)document.querySelector('.featured-strip').scrollBy({left:Number(target.dataset.slide)*450,behavior:'smooth'});
if(target.dataset.gallery!==undefined&&currentProduct){gallery=[{src:currentProduct.images[0],label:currentProduct.name},...currentProduct.additionalImages];galleryIndex=target.dataset.gallery==='main'?0:Number(target.dataset.gallery)+1;drawGallery();openDialog('#lightbox')}
if(target.dataset.lightboxStep){galleryIndex=(galleryIndex+Number(target.dataset.lightboxStep)+gallery.length)%gallery.length;drawGallery()}
switch(target.dataset.action){case 'menu':{const nav=document.querySelector('.mobile-nav');nav.hidden=!nav.hidden;target.setAttribute('aria-expanded',!nav.hidden);break}case 'search':openDialog('#search-dialog');setTimeout(()=>document.querySelector('#search-form input').focus(),0);break;case 'close':target.closest('dialog').close();break;case 'reset':filter={query:'',category:'Все',max:16000,sort:'curated',unique:false,new:false};location.hash='#/collection';route();break;case 'clear-data':favorites=[];cart=[];persist();toast('Избранное и корзина очищены');break;case 'export':{const text=['Тихие истории — моя подборка','Демонстрационная коллекция','',...cart.map(id=>{const p=products.find(p=>p.id===id);return `${p.name} — ${money(p.price)}`;}),'',`Итого: ${money(cart.reduce((sum,id)=>sum+products.find(p=>p.id===id).price,0))}`,'Оплата и доставка не подключены.'].join('\n');const url=URL.createObjectURL(new Blob(['\ufeff'+text],{type:'text/plain;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download='Тихие истории — подборка.txt';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);toast('Подборка сохранена файлом');break}}});
document.addEventListener('input',e=>{if(e.target.id==='catalog-search'){filter.query=e.target.value;results()}if(e.target.id==='price'){filter.max=Number(e.target.value);results()}});document.addEventListener('change',e=>{if(e.target.id==='sort'){filter.sort=e.target.value;results()}});document.addEventListener('submit',e=>{if(e.target.id==='search-form'){e.preventDefault();location.hash='#/collection?q='+encodeURIComponent(new FormData(e.target).get('q'));document.querySelector('#search-dialog').close()}});
document.addEventListener('keydown',e=>{if(document.querySelector('#lightbox')?.open&&['ArrowLeft','ArrowRight'].includes(e.key)){galleryIndex=(galleryIndex+(e.key==='ArrowRight'?1:-1)+gallery.length)%gallery.length;drawGallery()}});
let touchStart;document.addEventListener('touchstart',e=>{if(e.target.closest('#lightbox'))touchStart=e.touches[0].clientX},{passive:true});document.addEventListener('touchend',e=>{if(touchStart!==undefined&&e.target.closest('#lightbox')){const delta=e.changedTouches[0].clientX-touchStart;if(Math.abs(delta)>45){galleryIndex=(galleryIndex+(delta<0?1:-1)+gallery.length)%gallery.length;drawGallery()}touchStart=undefined}},{passive:true});
window.addEventListener('scroll',()=>document.querySelector('.header')?.classList.toggle('compact',window.scrollY>50),{passive:true});
document.addEventListener('pointermove',e=>{if(e.pointerType==='mouse'&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const hero=document.querySelector('.hero-image');if(hero&&scrollY<innerHeight){hero.style.setProperty('--px',`${(e.clientX/innerWidth-.5)*7}px`);hero.style.setProperty('--py',`${(e.clientY/innerHeight-.5)*5}px`)}}});
window.addEventListener('hashchange',route);route();
